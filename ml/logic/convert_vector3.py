# -*- coding: utf-8 -*- 
import os
from PIL import Image

class TestClass:

    def __init__(self, code, name):
        self.code = code
        self.name = name
    
    def getDatas(self,id):
        """
        
        """
        
        ##
        # DBから女優名を引き出し、画像にアクセスして画像を引き出す
        ##
        
        # files = ["faces/" + f for f in os ]
        labels = []
        datas = []
        
        for image in files:
            as_arrayed_img = np.asarray()
        
        
        
        print('test')
        return None